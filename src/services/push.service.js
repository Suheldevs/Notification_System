import admin from 'firebase-admin'
import webpush from 'web-push'

//app
export const PushToMobile = async(token, title, body, data = {})=>{
try{
    const message = {
        notification
    }
}
catch(err){
    console.log(err)
}
}